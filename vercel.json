{
  "installCommand": "true",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/extended/:path*",
      "destination": "https://api.starknet.extended.exchange/:path*"
    }
  ]
}
